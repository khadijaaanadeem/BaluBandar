# Production Environment Variables (remove .example from filename when deploying)
# Leave empty if no variables are needed for production

# Example variables (uncomment if needed):
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
